#include <iostream>
#include <algorithm>

#define VISITED true

using namespace std;

int main(void) {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	bool a = VISITED;

	cout << a;

	return 0;
}