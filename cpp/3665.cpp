#include <iostream>
#include <algorithm>

#define MAX_LEN 501

using namespace std;

int cache[MAX_LEN][MAX_LEN];
int T, n, m;

int main(void) {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);



	return 0;
}