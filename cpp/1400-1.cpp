#include <iostream>
#include <algorithm>
#include <vector>
#include <math.h>
#include <queue>

using namespace std;

struct State {

};

struct TrafficLight {
	char state;
	int ew, ns;
};

int main(void) {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int m, n;

	while ((cin >> m >> n) && (m != 0 || n != 0)) {
		vector<string> cache(m);
		vector<
	}

	return 0;
}